<!DOCTYPE html>
<html lang="en">
    {{>head}}
<body>
    {{>nav}}
    {{>header}}
    <main>
        <section>
            <h2>{{user.name}}</h2>
            <p>{{user.email}}</p>
        </section>
    </main>
    {{>footer}}
</body>
</html>